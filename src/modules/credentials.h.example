#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// WiFi Credentials
// Copy this file to credentials.h and fill in your actual WiFi credentials
// credentials.h is gitignored to keep your credentials safe
#define DEFAULT_SSID "your_wifi_ssid"
#define DEFAULT_PASSWORD "your_wifi_password"

#endif // CREDENTIALS_H
